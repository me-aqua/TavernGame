# TavernGame 设计备忘 · **历史与推导**

> 这份文档记录「**我们是怎么走到今天的**」—— 讨论过程、当时的推理、被否掉的方案、
> AI 提错过的记录、以及**已被取代的结论**。
>
> **它不是规范。** 当前有效的设计在 **`DESIGN.md`**，读那份就够了。
> 这份只有一个用途：**当有人问"为什么当初这么定"时，答案在这里。**
>
> 保留而不删除的理由：**决定会过时，来历不会** ——
> 不知道当初考虑过什么，就会把已经否掉的东西重新提一遍。
>
> 文中「第X节」指向的是 `DESIGN.md` 的节号。

---

## 一、讨论中得出的关键洞察

> ⚠️ **2026-09-14 状态标注**：本节是**讨论过程的沉淀**，其中数条已被后续决定**取代或修订**。
> 每条标题后标了当前状态 —— **别把标了 ⏳ 的当现成结论用**。
>
> | 洞察 | 现状 |
> | --- | --- |
> | 1 回合长度自适应 | 🔧 动机成立，实现改为"大纲决定跨度" |
> | 2 时间与事件是反馈环 | ⏳ **已被取代** —— 第三节明确剪成单向 `事件 → 时间` |
> | 3 同一 agent 被多次调用 | ⏳ **当前不适用** —— 5 个来源里 4 个已搁置 |
> | 4 中间状态 = 黑板模式 | 🔧 **部分修订** —— 机制保留且更重要，例子失效 |
> | 5 调用拓扑就是玩法 | ✅ 仍成立（但当前只有一种拓扑：纯线性） |
> | 6 引擎不规定创作结构 | ✅ 仍成立 |
>
> **保留而不删除的理由**：它们是**决定的来历** ——
> 删了以后，没人知道"当初为什么考虑过保持反馈环成环"。

### 洞察 1：回合长度应该是自适应的（动机）　🔧 实现已改

玩家输入"砍一刀"，**不能只模拟这 5 秒然后停下来等下一刀** —— 那太蠢了。

一个遭遇战应该被连演几个来回，演到一个值得让玩家重新决策的位置再停。

> ⚠️ **2026-09-14 更新**：实现方式已定 —— **"这一轮演到哪儿"交给大纲 agent 用自然语言表达**，
> 循环机制**当前不做**（见第三节）。下表里的"循环 3 次"只是当时的举例。

**动机与实现要分清：**

| | 内容 |
| --- | --- |
| **动机** | 固定切片（一次模型输出 = 一回合）会让节奏碎掉 |
| **最终实现** | **不是**引擎动态探索（那会引入"何时停"的判断难题），**而是作者在图里声明**（决定 #9/#16）：比如"战斗节点循环 3 次" |

也就是：**"自适应"这件事由作者的设计体现，而不是由引擎的智能体现。**

### 洞察 2：时间与事件是**反馈环**，不是流水线　⏳ 已被取代

```
时间 agent 判「过了多久」→ 需要知道「打了几个来回」
战斗 agent 判「几个来回」→ 每个来回都要知道「这之间过了多久」
```

旧做法是把环剪断：**先算时间、划定模拟范围，再让其他引擎在这个范围里跑**
（用户旧项目的做法，也是固定切片的来源）。

多 agent 架构的价值之一：**这个环可以保持成环** —— 让 agent 互相问。

> **这解释了"为什么同一个 agent 会被反复调用"**：不是设计缺陷，是反馈环的必然结果。

> ⏳ **2026-09-14：这条的结论已被取代。**
> 第三节明确写了「**反馈环被剪成了单向 `事件 → 时间`**」——
> 先由大纲定"要发生什么"，再由时间判"花了多久"。
>
> | | 现状 |
> | --- | --- |
> | **观察** | ✅ 仍然成立 —— 时间和事件确实互相依赖 |
> | **处方** | ❌ 已放弃 —— 我们**不再**让它保持成环，而是**选定一个方向** |
>
> 连带失效的还有最后那句"同一 agent 会被反复调用"：
> **纯线性图下，每个节点每轮只调一次。**

### 洞察 3：同一 agent 被多次调用的**五个**来源　⏳ 当前不适用

| 来源 | 机制 | 例子 |
| --- | --- | --- |
| 常规工序 | 每轮都要问 | 时间 agent |
| 聚合调用 | 同类判断有多个对象 | 场景里 3 个 NPC → 角色 agent 调 3 次 |
| **条件触发** | A 的输出决定要不要走 B | 战斗判"重伤" → 才需要评估"伤势对后续行动的影响" |
| **多阶段流程** | 同一件事先后问两次 | 先"预判后果" → 执行 → 再"结算实际发生" |
| **采样** | 同一件事**要多个候选** | 故事 agent 产出多个大纲，挑最好的（见洞察 6） |

后三个是网状结构的真正来源：**串并联只能表达固定顺序，网状才能表达"结果决定路径"。**

> ⏳ **2026-09-14：当前不适用。** 上表五个来源里，**四个已被搁置** ——
> 聚合调用（扇出）、条件触发、多阶段流程、采样。示例卡的默认图是**纯线性 8 节点**，
> 每轮每个节点**只调一次**（见第三节），只剩第一行「常规工序」还活着。
>
> 💡 **但别删**：它是**将来加机制时的需求清单** ——
> 哪天真有作者要写"七层塔，每层同样流程"，这份清单能告诉他要加的是哪一种机制。

### 洞察 4：中间状态 = 黑板模式　🔧 部分修订

```
图执行期间（内存工作副本，不落盘）      图执行完毕后（提交为权威状态）
轻伤 → 重伤 → 骨折                  只存 左臂骨折
```

**两个东西要分清**（此前文档把它们混在一起，是错的）：

| | 工作副本 | 权威状态 |
| --- | --- | --- |
| 活在哪 | 内存，本轮有效 | 存档，跨轮有效 |
| 谁能读 | **本轮所有 agent** | 下一轮的 agent（作为起点） |
| 谁能写 | 各 agent 的输出经主 agent 合并后写入 | 只有"提交"这一步写 |
| 轮末 | **丢弃中间态**，只留最终值 | 成为下一轮的起点 |

- **写冲突的防线**：中途写的是工作副本，"谁能改权威状态"只有一条路（提交），
  所以不需要复杂的锁机制 —— 冲突只会发生在"同一个字段本轮被两个 agent 都改了"，
  这由**主 agent 合并**时解决（见待定 5）。

> 🔧 **2026-09-14：机制保留，例子失效 —— 而且它比原来更重要了。**
>
> | | 现状 |
> | --- | --- |
> | **死掉的是例子** | `HP 100 → 88 → 61 → 44` 是"**回合内反复模拟**"的产物。没有循环、没有战斗 agent，这种回合内抖动**不会再出现** |
> | **活着的是机制** | **工作副本 → 提交**，它现在的身份是**事务边界** |
>
> **为什么反而更重要**：如果 ④ 地图 直接写权威状态，而 ⑥ 故事 那一步报错，
> **世界就更新了一半** —— 时间跳了、地图多了一层，玩家却没看到任何剧情，
> 刷新一下又退回去，下一轮成功时把"半个回合"一起持久化。
>
> 这正是 `AGENTS.md` 待办里**第 1 条**那个已知缺陷（"回合开头就改内存、`save()` 在末尾"）的
> **根因**，也是它的正解。所以本节真正该叫「**提交 = 事务边界**」，
> "中间状态"那半截降级成历史注脚。

### 洞察 5：调用拓扑就是玩法　✅ 仍成立

同一个世界、同一批 agent，**换个拓扑就是另一个游戏**：

| 游戏类型 | 拓扑特征 |
| --- | --- |
| 恋爱模拟 | 无战斗 agent；角色 agent 高频多次；存在性条件看好感度 |
| 侦探推理 | 时间权重极高（时限压力）；"证据齐了才能指认"是条件门槛 |
| 生存恐怖 | 时间 + 资源消耗每轮必跑；"受伤→感染→恶化"是链式触发 |

**所以卡作者写的不是"设定"，是"规则拓扑"。**

### 洞察 6：引擎不规定「创作结构」　✅ 仍成立

「故事要几个 agent、先写大纲还是直接写、要不要多方案挑选」——
**这是创作方法论，不同作者有不同做法**（用户 2026-09-13 明确：先不预设）。

引擎只提供机制（能并行、能条件、能循环、能采样）—— ⚠️ 这是**目标能力**；2026-09-14 定：示例卡先用**纯线性 8 节点**，条件 / 循环 / 扇出 / 采样暂不实现（见第三节），
**具体怎么组织创作，留给卡**。

> 这条是洞察 5 的延伸：不但"调用拓扑"是玩法，**"创作流程"也是玩法的一部分**。

---

## 二、踩过的坑（别再犯）

| 坑 | 后果 | 教训 |
| --- | --- | --- |
| **单模型一轮里扮演多个引擎**（旧项目的"人格分裂技术"） | 引擎一多，超出单轮输出能力：质量崩、格式飘 | **职责分离要靠独立调用，不是靠提示词里排队** |
| 把规则写进引擎 | 玩家只会用"引擎认识的动词"，自由度死掉 | 规则交给 AI 判断 |
| 把"引擎持有事实"理解成"引擎计算一切" | 引擎越来越重，越来越像传统游戏 | 引擎只**存**事实，不**算**事实 |
| **把叙事性判断写成引擎规则**（如"关键时刻检测器"） | 又变成 `if HP<30` 那套，且规则永远不够用 | 叙事判断交给提示词（决定 #14） |
| **概念改了但文档别处没跟着改**（本次修复的起因） | 同一文档里"关键时刻"有两个矛盾含义，下次读的人必错 | 改决定后要**全文扫描**旧概念残留 |

---

## 三、来历与提错记录

> 这一节收集**各处决定背后的"当时发生了什么"** —— 尤其是 AI 提错、被用户否掉、
> 以及被后续决定推翻的记录。它们从 `DESIGN.md` 里搬出来，
> 因为**读当前设计的人不需要看这些**。

### #10 的反例（留档防复发）

「战斗命中 → 才调角色 agent」是**错的** ——
挨了一刀哪怕躲过去，角色也有反应（心跳、重新评估对手、退半步）。

### 图的权力结构：AI 曾把「分工」写成「分权」

AI 曾列过一张"决定权边界"表，写着大纲
「不能碰：场景长什么样 / 角色是谁 / 花了多久」。**用户否掉了**：

> *「大纲顺手把场景和角色编了其实是很合理的，这就是大纲存在的意义。
> 如果大纲 agent 觉得需要让玩家遇到一个红头发爆乳美女铁匠，
> 那角色 agent 就应该服从。」*

### 循环：完整推导，以及"什么时候才真需要"

最初的担心是「线性图 + 无循环 = 砍一刀停一下」。**用户指出这个担心是多余的**：

> *「现在一轮砍一刀的问题是由大纲 agent 来避免的，而且现在我们没有战斗 agent，
> 也不会出现循环调用三次的问题。」*

**循环什么时候才真的需要**（都不是现在）：战斗回合制（每回合都要过判定 + 角色）/
对称重复结构（三日仪式、七层塔、走十天的每日结算）/
强制穷举（每个嫌疑人审一遍 —— 严格说这是**扇出**不是循环）。

**循环的原始动机已经不存在了**：它当初是为"调用次数可预测、防演化失控"（决定 #9），
但线性图的成本上限天然就是 `节点数 × 1`。所以循环现在只剩**表达力**价值。

⚠️ **连带撤回 AI 当时的一句话**：AI 曾说"卡格式里必须给循环留位置"。
这**违反项目自己的"不提前预留"原则**（见 `AGENTS.md` 的奇幻历法教训），
而且**现在一张卡都不存在** —— 将来加字段的迁移成本是零。**所以不留位。**

### 玩家 / 主控：一次术语修正（2026-09-14）

用户指出必须区分**玩家**与**主控**。在此之前，文档里把两者揉成了一个词"玩家"，
于是写出了「**玩家在世界之内**」这种自相矛盾的话。已按新术语统一。

### 移动耗时：AI 曾主张「写死的数字不能违反」

AI 曾主张"描述里写死的数字是**事实**，时间 agent 不能违反"。**用户否掉了**：

> *「不用这么死。徒步半小时被判定成 3 小时，首先 AI 不会这么笨；
> 其次如果他真决定这么做了，说明半路肯定出事了 —— 正好作为新的游戏内容提供给玩家。
> 我倒更担心一旦写死半小时，每轮就没有任何区别都是半小时，
> 这部分游戏内容就再也无法产生新鲜感了。」*

### 开局：作者曾写死开场白（2026-09-14 推翻）

早期 `DESIGN.md` 写的是「**开局故事由卡作者规定** —— 作者写好开场，不需要引擎生成」，
示例卡里也就真的写了一段固定开场（旅店 + 炖菜 + 悬赏令 + 两个佣兵）。

**用户推翻了**：

> *「首先开局文本不要写死，我想每轮让 AI 生成不一样的，
> 就给个初始时间地点和主控信息就行了。」*

理由跟「移动耗时不能写死」**是同一个**：**写死的东西每轮都一样，新鲜感就再也产生不了。**

现改为：开局块只给事实（时刻 / 地点 / 主控状态），另加一份 `开局要求`（写**要求**不写**正文**）。

> ⚠️ 这跟坑 #7「示例是模型的行为模板」**不冲突**，但值得记一笔区别：
> **样板教它怎么写，范文替它写了。**
> 我们留下的例子是角色样板（萨伦的生平性格、老板娘怎么说话），撤掉的是整段开场白。

### 节点数据流：一句错话在渲染器里潜伏了（2026-09-14）

渲染出来的卡里写着：

> 「每个节点拿到的上下文完全一样（**四块**设定 + 核心剧本 + 全部历史 + 当前状态 + 玩家这一轮的原话）」

用户读到这里觉得不对，问 AI 是不是这个意思。**查下来三个问题叠在一起：**

| # | 问题 |
| --- | --- |
| 1 | **内容就是错的** —— 卡自己的节点提示词全都在引用上下游（④读③、⑥读⓪、⑦读②③⑤），"完全一样"跟卡打架 |
| 2 | **它不在卡 JSON 里** —— 是 `render-card.mjs` 硬编码的一段正文，md 本该是纯渲染产物 |
| 3 | **真正的约定没人写** —— `DESIGN.md` 全文 0 次「上游」、0 次「下游」、0 次「上一个节点」 |

第 3 条最要命：**九个节点的图，最基础的问题「数据怎么流」从来没被写下来过。**
所以第 1 条那句话才会一直没人发现是错的 —— **没有正本可以对照。**

现在约定写死在卡的 `节点约定` 字段里，`DESIGN.md` 也补了一节，校验器加了检查。

> 📌 **教训**：md 里出现的**任何一句关于卡的事实**，都必须来自 JSON。
> 渲染器只该管**结构**（标题、表头、分隔线），不该管**内容**。
> 已经顺手把「五块」「9 节点」这类数字也改成从 JSON 数了。

### 钱：AI 曾建议「不拆」

AI 曾建议"金钱可以留着不拆"（理由是拆了添乱）。**用户否掉了**：

> *「别存金钱，就存银币金币吧，如果玩家找到一个宝石，
> 那他就得找地方换成铜币，不然花不出去**还会被人偷**。」*

—— 理由比 AI 的强：**具体硬币能被偷，抽象数字不能。**

---


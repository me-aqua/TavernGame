# ============================================================
#  .gitignore —— 告诉 Git「哪些文件不要纳入版本管理」
#
#  为什么需要它？
#  Git 会把仓库里所有文件都记录下来。但像临时文件、
#  依赖包、编辑器缓存这些东西，记录它们只会让仓库变脏、
#  变慢，甚至泄漏你的隐私。这个文件就是排除清单。
#
#  语法：每行一个规则，# 开头是注释，! 开头表示「例外」
# ============================================================

# --- 本项目的本地工具与临时备份（属于个人环境，不需要上传）---
.tools/
PATH-backup.txt

# --- 依赖包目录：用 package.json 就能还原，不必上传 ---
node_modules/
vendor/

# --- 构建产物：由源代码生成，可以随时重新生成 ---
dist/
build/
*.min.js

# --- 日志与缓存 ---
*.log
npm-debug.log*
.npm/
.cache/

# --- 编辑器 / IDE 的个人配置 ---
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# --- 操作系统自动生成的垃圾文件 ---
# 这些是 Windows / macOS 自己产生的，和你的项目无关
Thumbs.db
Desktop.ini
.DS_Store

# --- 敏感信息：绝对不要上传！---
.env
.env.local
*.pem
*.key

# --- 关于 API key ---
# 现在项目是纯前端：API key 存在浏览器的 localStorage 里，
# 不会出现在这个文件夹，所以不需要在这里排除什么。
# 但要记住：永远不要把 key 写进任何被提交的文件。

# --- 项目所有者的个人档案：不公开 ---
ME-AQUA.md


# ============================================================
#  .gitignore —— 告诉 Git「哪些文件不要纳入版本管理」
#
#  语法：每行一个规则，# 开头是注释，! 开头表示「例外」
# ============================================================

# --- 依赖包目录：用 package.json 就能还原，不必上传 ---
node_modules/
vendor/

# --- 构建产物：由源代码生成，可以随时重新生成 ---
#   ⚠️ dist/ 永远不进仓库 —— 发布由 GitHub Actions 构建
dist/
build/
*.min.js
*.tsbuildinfo

# --- 测试覆盖率报告（HTML 报告有几百个文件，属于生成物）---
coverage/

# --- 本地工具与临时备份（属于个人环境，不需要上传）---
.tools/
PATH-backup.txt

# --- 日志与缓存 ---
*.log
npm-debug.log*
.npm/
.cache/
.vite/

# --- 编辑器 / IDE 的个人配置 ---
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# --- 操作系统自动生成的垃圾文件 ---
Thumbs.db
Desktop.ini
.DS_Store

# --- 敏感信息：绝对不要上传！---
.env
.env.local
*.pem
*.key

# --- 关于 API key ---
# 项目是纯前端：API key 存在浏览器的 localStorage 里，
# 不会出现在这个文件夹，所以不需要在这里排除什么。
# 但要记住：永远不要把 key 写进任何被提交的文件。

# --- 项目所有者的个人档案：不公开 ---
ME-AQUA.md

# 生成物与第三方内容，不格式化
dist/
coverage/
node_modules/
package-lock.json
public/mea-avatar.jpg

# 文档里有大量手工排版的中文行宽，Prettier 会打乱（proseWrap: preserve 已尽力，仍不动它们）
doc/
.agents/

# 钩子脚本没有扩展名，Prettier 按 JS 猜；而且**绝不能自动格式化它们** ——
# 在提交中途改动钩子自身会让本次提交的行为变得不可预测
.githooks/commit-msg
.githooks/pre-commit
.githooks/pre-push

# 卡与它的渲染产物：手工排版（一行一条，diff 干净）—— 重排会毁掉「改卡能被评审」这个选型理由
cards/

# 检查脚本与三道钩子同性质：它们在提交中途被执行，绝不自动格式化
.githooks/checks/

# TavernGame 设计备忘

> 这份文档记录**设计层面的决定与推导**，不写代码。
> 起因：2026-09-13 的一轮长讨论 —— 从「时间是不是该交给子 agent」一路聊到
> 「整个项目到底是什么」。
>
> **用法**：动手做任何架构改动前先读这份。里面区分了
> **已决定** / **待定** / **旧架构（待替换）**，别把第三类当成第一类。
>
> **维护约定（用户 2026-09-13 授权）**：讨论出新结论就随时更新本文档，
> 不必每次询问。只有"要动代码"或"改项目身份"时才需要先问。

---

## 一、项目定位：这是什么东西

### 一句话

**一个可持续演化的世界模拟器；「回合」只是它的人机接口。**

世界在后台连续演化，玩家每隔一段被叫停一次，获得干预的机会 ——
然后世界继续跑。玩家看到的不是"世界本身"，而是**被剪辑过的关键时刻**。

### 两个产品级目标

| 目标 | 含义 | 设计含义 |
| --- | --- | --- |
| **① 不写死规则** | AI 游戏相对传统文字游戏的唯一护城河：**不需要穷举** | 只要开始写「XX 造成 YY 点伤害」，项目就退化成传统文字游戏 |
| **② 让能力强的作者脱颖而出** | 用户明确在意：**卡的表达能力就是产品竞争力** | 卡格式必须留足表达空间 —— 高手能写精细的规则，新手能用预设 |

> ⚠️ **目标 ② 有内在张力**：既要对新作者友好，又要给强作者空间。
> 解法方向是**分层**（预设卡 → 配置模式 → 完全自定义），
> 但**不能为了友好而阉割表达力** —— 那会同时杀死目标 ② 和项目的差异化。

### 传统规则表为什么会死

```
刀伤 = HP-15、钝击 = HP-8、火伤 = 每回合 -3 …
```

玩家输入「我用湿毛巾抽他的脸」→ 规则表里没有 → 要么不认，要么硬套近似规则。

**致命后果**：玩家会迅速学会「只有引擎认识的动词才有真实后果」，
然后退回去只做那些动作 —— **自由度就是这么死的**。

而模型见过这个世界。**它的"规则表"是它读过的全部文本。**

### 项目不是在做什么

- ❌ 不是"更好的酒馆"（轮次对话 + 角色卡）
- ❌ 不是"带规则引擎的 AI 文字游戏"（规则写死，只是藏进代码）
- ❌ 不是"单模型扮演多个引擎"（见「踩过的坑」）

---

## 二、已决定的设计（截至 2026-09-13）

| # | 决定 | 谁提出 | 理由 |
| --- | --- | --- | --- |
| 1 | **规则由 AI 判断，不写死事件表** | 用户 | 写死规则 = 退化成传统文字游戏 |
| 2 | **每个引擎交给独立 agent**，不做"单模型一轮演多角色" | 用户 | 旧项目已验证：引擎一多就超出单轮输出能力，质量与格式双崩 |
| 3 | **主 agent 只做分发 / 汇总 / 格式校对**，不写故事 | 用户 | 职责分离；它的提示词全是格式约束，更短更稳 |
| 4 | **写故事的另设 agent**（数量与分工由卡决定，见 #9） | 用户 | 叙事质量与编排逻辑是两种能力 |
| 5 | **启用哪些 agent 由「卡」决定** | 用户 | 恋爱模拟就不装战斗 agent；没有战斗 → 状态里根本没有血量字段 |
| 6 | **状态必须存"准确值"** | 用户 | 不存的话下一轮模型不知道发生过什么 |
| 7 | **中间状态不必保留，只存关键时刻的状态** | 用户 | HP 100→88→61→44，中间那三个值对游戏没有意义 |
| 8 | **"回合"是人为的玩家干预点**，不是世界的属性 | 用户 | 世界连续演化，"停下来等玩家"只是为了参与感 |
| 9 | **关键时刻的判定规则由卡作者写死** | 用户 | agent 自荐"可以停了"会互相推诿、永不满足；把裁量权交给作者 |
| 10 | **调用拓扑由卡作者写死**（含条件分支与循环） | 用户 | 「写死就固定了」—— 拓扑即玩法，作者理应有这个控制权 |
| 11 | **上下文默认全敞开给所有 subagent** | 用户 | 信息对称，避免各算各的；成本换准确性 |
| 12 | **卡可以自定义 agent 本身**（名字、提示词、职责、读写哪些状态） | 用户 | 世界状态量都由作者定，agent 自然也该由作者定 |
| 13 | **故事创作由多个 agent 协作完成，具体几个、各干什么，也由卡决定** | 用户 | 不同作者有不同想法，**引擎不规定创作结构** |

### 由 #11 派生的一条实现建议（不是决定）

执行器给每个 agent 传上下文时，**走一个统一的函数**（如 `取上下文(agent名)`），
而不是在各处写死"传全部"。将来要按 agent 裁剪上下文时，只改这一个函数。
**现在多写几行，将来省一次重构。**

---

## 三、讨论中得出的关键洞察

### 洞察 1：「回合」是接口，不是时钟

玩家输入"砍一刀"，**不能只模拟这 5 秒然后停下来等下一刀** —— 那太蠢了。

引擎应该自己评估：**这是不是一场遭遇战的开始？该不该连演几个来回，
到某个关键时刻再交给玩家决策？**

于是"一回合有多长"不再是固定切片，而是**由演化过程动态决定**（何时停则按 #9，由卡写死）。

### 洞察 2：时间与事件是**反馈环**，不是流水线

```
时间 agent 判「过了多久」→ 需要知道「打了几个来回」
战斗 agent 判「几个来回」→ 每个来回都要知道「这之间过了多久」
```

旧做法是把环剪断：**先算时间、划定模拟范围，再让其他引擎在这个范围里跑**
（用户旧项目的做法，也是固定切片的来源）。

多 agent 架构的价值之一：**这个环可以保持成环** —— 让 agent 互相问，直到收敛。

> **这解释了"为什么同一个 agent 会被反复调用"**：不是设计缺陷，是反馈环的必然结果。

### 洞察 3：网状调用有**五个**来源

| 来源 | 机制 | 例子 |
| --- | --- | --- |
| 常规工序 | 每轮都要问 | 时间 agent |
| 聚合调用 | 同类判断有多个对象 | 场景里 3 个 NPC → 角色 agent 调 3 次 |
| **条件触发** | A 的输出决定要不要走 B | 战斗 agent 判"重伤" → 触发角色 agent 评估行动力 |
| **多阶段流程** | 同一件事先后问两次 | 先"预判后果" → 执行 → 再"结算实际发生" |
| **采样** | 同一件事**要多个候选** | 故事 agent 产出多个大纲，挑最好的（见洞察 6） |

后三个是网状结构的真正来源：**串并联只能表达固定顺序，网状才能表达"结果决定路径"。**

### 洞察 4：中间状态 = 黑板模式

```
执行期（内存工作副本，不落盘）        收敛期（提交为权威状态）
HP 100 → 88 → 61 → 44              只存 HP 44
轻伤 → 重伤 → 骨折                  只存 左臂骨折
```

- 所有 agent 读写**同一份工作副本** → 天然互相可见，不用为"谁先算"发愁
- 一轮演化结束后，**只有最终态**被提交为权威状态
- **这同时解决了**「状态由谁写」和「中间量要不要存」两个问题

### 洞察 5：调用拓扑就是玩法

同一个世界、同一批 agent，**换个拓扑就是另一个游戏**：

| 游戏类型 | 拓扑特征 |
| --- | --- |
| 恋爱模拟 | 无战斗 agent；角色 agent 高频多次；触发条件看好感度阈值 |
| 侦探推理 | 时间权重极高（时限压力）；"证据齐了才能指认"是条件门槛 |
| 生存恐怖 | 时间 + 资源消耗每轮必跑；"受伤→感染→恶化"是链式触发 |

**所以卡作者写的不是"设定"，是"规则拓扑"。**

### 洞察 6：引擎不规定「创作结构」

「故事要几个 agent、先写大纲还是直接写、要不要多方案挑选」——
**这是创作方法论，不同作者有不同做法**（用户 2026-09-13 明确：先不预设）。

引擎只提供机制（能并行、能条件、能循环、能采样），
**具体怎么组织创作，留给卡**。

> 这条是洞察 5 的延伸：不但"调用拓扑"是玩法，**"创作流程"也是玩法的一部分**。

---

## 四、图执行器与卡的格式

### 「图执行器」是什么（写给非程序员）

**图** = 一张"谁在什么时候被调用"的关系图：

```
        ┌─► 时间 agent ─┐
输入 ───┼─► 战斗 agent ─┼─► 汇总 ─► 故事 agent
        └─► 角色 agent ─┘
                ▲            │
                └──(条件)────┘      ← 回头路 / 条件分支
```

**执行器** = 照着这张图跑的程序。它负责：

| 职责 | 具体表现 |
| --- | --- |
| 发起调用 | 按图叫 agent，把该给的数据给它 |
| **判断走哪条路** | 「战斗 agent 说重伤了」→ 才叫角色 agent；没说 → 跳过 |
| 并行 | 三个 NPC 互不依赖 → 同时发请求，而不是排队 |
| 循环 | 「反复演化直到关键时刻」—— 这个"反复"由它管 |
| 汇总 | 收集各 agent 的结果给主 agent |
| 兜底 | 调用次数上限（防网状结构转不停） |

**没有执行器会怎样**：那些"先调谁、什么条件下调下一个"的逻辑，
就得散落在每个 agent 的提示词里各写一遍 —— 这正是"人格分裂"失败的一部分原因。

**体积预期**：不是重型框架，就是**一张声明表 + 一两百行 JS**。

### 拓扑长什么样（示意）

```json
{
  "拓扑": [
    { "agent": "时间", "上下文": "全部" },
    { "agent": "战斗", "上下文": "全部", "并行": true },
    { "agent": "角色", "上下文": "全部", "当": "战斗.结果 == '命中'" },
    { "循环": { "直到": "主agent.判定为关键时刻", "最多": 6 } }
  ]
}
```

> **关键：「写死拓扑」不等于「只能直线执行」** —— **条件本身就是拓扑的一部分**。
> 作者写死的是"什么时候调谁、什么条件跳哪一步"，既固定又可表达。

### ⚠️ 硬约束：卡必须是**声明式**格式（JSON / YAML），不能是代码

理由：**用户是代码小白 —— 而他的用户多半也是。**
如果做卡要写 JavaScript，那 99% 的人做不了卡，"既能做卡"就废了，
**产品目标 ②（让强作者脱颖而出）也无从谈起**。

声明式的好处：作者改的是文字和数据；我们能校验格式；将来能加图形编辑器。

```json
{
  "agents": {
    "战斗": {
      "提示词": "你负责判定战斗的成败与伤情…",
      "读": ["HP", "装备", "伤势"],
      "写": ["HP", "伤势"],
      "输出格式": { "命中": "bool", "伤害": "number", "部位": "string" }
    }
  },
  "拓扑": [ "…" ]
}
```

---

## 五、待定问题

> ⚠️ 这些**没有结论**，动手前必须先定。

| # | 问题 | 备选 | 状态 |
| --- | --- | --- | --- |
| 1 | **演化预算怎么设？** 一轮最多多少次调用、多少 token | 硬上限 / 按卡配置 | 🆕 待定 |
| 2 | **执行器的拓扑 schema 具体长什么样** | 自研轻量 JSON / 现成图执行库 | 🆕 待定 |
| 3 | **"谁挑大纲"等采样细节** | 主 agent 挑 / 独立评委 agent / 卡配置 | 🆕 待定（洞察 6 已定"不预设结构"，但机制要能支持） |
| 4 | **状态校验的粒度** | 只做范围检查 / 也做引用完整性 / 也做语义检查 | 🆕 待定 |
| 5 | **工作副本的并发读写规则** | 串行 / 并行 + 冲突解决 | 🆕 待定（洞察 4 给了方向） |
| 6 | **卡格式的版本迁移** | 卡格式变了，老卡怎么办 | 🆕 待定 |

### 已解决（原待定，现已有答案）

| 原问题 | 结论 | 出处 |
| --- | --- | --- |
| 演化什么时候停 | **卡作者写死判定规则** | 决定 #9 |
| 子 agent 上下文给多少 | **默认全敞开**（将来可加裁剪开关） | 决定 #11 |
| 卡的自定义 agent 有多深 | **完全自由**（但必须声明式） | 决定 #12 |
| 故事 agent 什么时候写 | **不预设** —— 创作结构由卡决定 | 决定 #13 / 洞察 6 |

---

## 六、已知风险

### 风险 1：网状 + 条件触发 → 可能无限循环

```
战斗 agent →「NPC 重伤」→ 角色 agent →「行动力下降」
  → 战斗 agent →「他防不住了」→ 角色 agent →「又受伤了」→ …
```

**必须在设计期就有终止条件。** 现有代码的 `maxSteps`（默认 10）是同类机制的前身，
但网状架构需要更完整的：单 agent 每轮调用上限、总调用上限、
**以及卡作者写的"关键时刻"本身可能就是死循环的源头**（规则写错 → 永远到不了关键时刻）。
→ 所以待定问题 1（演化预算）是**硬需求**，不是优化。

### 风险 2：成本随 agent 数量线性增长

```
5 个 subagent × 3000 token 上下文 = 每轮 15000 token 输入
（对比：现在单 agent 约 6000）
```

**真正的 token 大头不是"多几次调用"，而是"上下文被复制了几份"。**
决定 #11 选了"全敞开"，代价就是这个 —— 用成本换准确性，**用户已明确接受**。

### 风险 3：多 agent 的一致性

- 同一事件被两个 agent 各自判断 → 出现两个版本（如都算伤害，一个说 12 一个说 8）
- → 方向已定（洞察 4：单一工作副本 + 收敛后提交），但**并发读写细节未定**（待定 5）

### 风险 4：作者写不好规则，游戏就不好玩

决定 #9 把"节奏"交给了作者的规则质量。这是**有意的**（目标 ②），
但意味着**预设卡模板的质量直接决定新手体验** —— 不能只做机制、不做示范。

---

## 七、踩过的坑（别再犯）

| 坑 | 后果 | 教训 |
| --- | --- | --- |
| **单模型一轮里扮演多个引擎**（旧项目的"人格分裂技术"） | 引擎一多，超出单轮输出能力：质量崩、格式飘 | **职责分离要靠独立调用，不是靠提示词里排队** |
| 把规则写进引擎 | 玩家只会用"引擎认识的动词"，自由度死掉 | 规则交给 AI 判断 |
| 把"引擎持有事实"理解成"引擎计算一切" | 引擎越来越重，越来越像传统游戏 | 引擎只**存**事实，不**算**事实 |

---

## 八、术语表

| 术语 | 在本项目里的含义 |
| --- | --- |
| **agent** | 一次独立的模型调用（纯前端项目里没有进程，agent 就是一次 API 请求） |
| **引擎** | 持有世界状态、做一致性校验的代码层（**不算规则**） |
| **卡** | 一局游戏的声明式配置：世界、角色、agent 定义、拓扑、关键时刻判定 |
| **关键时刻** | 世界演化到"值得让玩家决策"的那个点；**判定规则由卡写死**（决定 #9） |
| **工作副本** | 演化过程中所有 agent 读写的那份内存状态；不落盘，收敛后只提交终态 |
| **拓扑** | agent 之间的调用关系：串行 / 并行 / 条件触发 / 循环 / 采样 |
| **图执行器** | 照着卡里的拓扑跑调用的程序（声明表 + 一两百行 JS） |

---

## 九、与现有代码的关系

> **重要**：本文档描述的架构与**当前代码（v0.5.4）不是一回事。**

| | 当前代码 | 本文档描述的目标 |
| --- | --- | --- |
| agent 数量 | 1 个 | 主 agent + 按卡声明的 N 个 |
| 判定 | 单模型顺手判 | 专职 agent 判断 |
| 工具 | 1 个（`advance_time`） | 由卡声明（未来） |
| 回合长度 | 一次模型输出 | 动态演化到关键时刻（规则由卡写死） |
| 引擎职责 | 校验 + 持有时间状态 | 存储 + 校验（**不计算**） |

**当前代码是"能跑通的最小验证"，不是终态。**
迁移路径待定，但有一条已经清楚：**`advance_time` 的那个位置，将来属于时间 agent。**

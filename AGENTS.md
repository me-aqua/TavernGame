# AGENTS.md

纪律全部自动化：`.githooks/` 在提交时强制检查；**新增纪律 = 新增检查，不写文字**。

## 行为规则

1. 不要假设。不要隐藏困惑。主动暴露权衡取舍。
2. 只写解决当前问题的最小代码，不做任何推测性功能。
3. 只修改必须改的地方，只清理自己产生的问题。
4. 明确定义成功标准，验证通过前持续迭代。
5. 同一研究任务的修正、补全和范围扩展必须在原任务目录内演进，不另建平行任务目录。

## 反过度工程 / 反防御性编程

- 信任内部代码和框架保证。
- 只在系统边界（用户输入、外部 API、网络）做校验。
- 禁止为「不可能发生」的场景添加错误处理、回退、空值检查或验证。
- 绝不吞掉错误（禁止 rescue nil / 宽泛 catch / 静默默认值）。
- 禁止为一次性操作创建辅助函数、工具类或抽象。
- 优先快速失败，而不是掩盖问题。

## 文档地图（`doc/`）

| 文件                    | 内容                                                   | 什么时候读                   |
| ----------------------- | ------------------------------------------------------ | ---------------------------- |
| `doc/DESIGN.md`         | **当前设计**：决定表 #1–#45 + 每条决定的推导           | **改架构前必读**             |
| `doc/DESIGN-HISTORY.md` | 历史与推导：被取代的结论、提错过的记录、当年的备选方案 | 只在问「当初为什么这么定」时 |
| `doc/CHANGELOG.md`      | 每个版本改了什么（含未发布的做卡系统）                 | 发版前、想知道「哪一版变的」 |

## Skill 地图（`.agents/skills/`，按需加载）

| skill           | 管什么                                                               | 什么时候用                            |
| --------------- | -------------------------------------------------------------------- | ------------------------------------- |
| `environment`   | 运行环境事实：工作区、工具链、代理、本机踩过的坑                     | 跑命令 / 连服务 / 报错像别的问题时    |
| `checks`        | 自动化检查体系：三层门禁各跑什么、阈值怎么定                         | 改钩子、或想知道「为什么被拦」        |
| `release`       | 发布流程：远端、提交身份、Pages 的 Actions 前置、标签顺序            | 提交 / 推送 / 打标签 / 发版           |
| `testing`       | 测试纪律：各层测什么、门禁在哪、「新增功能必须新增测试」             | 写功能、改行为、判断「改坏没有」      |
| `ui-testing`    | 自动化 UI 测试：四层结构、Storybook + Playwright、结构判据与像素基线 | 改界面 / 加组件 / 调布局或文案        |
| `verification`  | 验证纪律：怎么定义成功标准、四类「看起来通过其实测错了」             | 报告「全绿」之前                      |
| `browser-debug` | 用 CDP 在真实 Chrome 里读运行时 DOM                                  | 确认「页面真的能跑」而不只是 HTTP 200 |
| `comments`      | 注释规范：文件头与函数注释、为什么不写历史对比                       | 写注释、或注释被 lint 拦下            |

## 卡

`cards/morningwind.json` 是示例卡的**唯一事实来源**；`cards/晨风镇.md` 是 **`npm run render:card` 渲染出来的本地视图**（别手改，**不进仓库** —— 派生物进了仓库就多出一份要同步的真相）。
引擎侧进度：卡已能解析 / 校验 / 按图执行，并已接管**新游戏的第一帧**（起始时刻 / 场景 /
默认名，决定 #42）；**回合已按卡里的图跑** —— 拓扑里每个节点各调一次模型（示例卡九个），
叙事取故事节点的正文、时间由时间节点的 JSON 产出推进（阶段 6c，无 tools）——
**显示也已由卡驱动** —— 顶栏条目与侧栏块按 `声明.显示` 渲染（阶段 6b，决定 #45）。
设计见 `doc/DESIGN.md`（第四 / 五节 + 决定 #25–#45）。

## 快速入口

- 本地全量把关：`npm run verify`
- 同一套检查在 CI 上也会跑（PR 与 push 到 main）：`.github/workflows/ui.yml`
- 界面相关另跑：`npm run visual`（整页截图 + 结构检查）、`npm run stories`（组件故事）
- 跳过（需在提交信息里说明理由）：`SKIP_DISCIPLINE=1 git commit ...`
- 具体机制见对应实现：`.githooks/`（检查）、`doc/DESIGN.md`（设计）、`.agents/skills/`（上表）

/**
 * 段 7b 的锚点表：**6 张旧表的 `applies`**（这一整片内容在什么场合下生成）+ 它现在该落在哪，一条一行。
 *
 * 为什么存成文本而不是 `.ts`：这些是**中文数据**（applies 原文与表名），
 * 而 `.githooks/checks/ascii.mjs` 对**代码文件**（含 `tests/**`）要求 ASCII。
 *
 * ⚠️ **这份表是 S1 从改动前的卡里抄下来的** —— 原文取自 `git show c8b2052^:cards/<卡>.json`
 *    的 `generators[].applies`（`c8b2052` 是段 7a 把那张表删掉的那一笔提交；
 *    所以"改动前"= `c8b2052^`）。⚠️ S0 写的取法是 `275411f^` —— 那一笔**已经在删表之后**，
 *    里面没有 `generators`（实测），本文件按 git 历史取。
 *
 * 归属来历（**推导是组长的，不是这里定的**）：
 *   · S0 §七 F4：applies 讲的是"**这一整片内容在什么场合下生成**"，归**主提示词**、
 *     **不归动作的 `whenToUse`**；
 *   · 表本身归哪个节点，照段 7a 已经拍过的归属（`generator-principles.txt` 的第 4 列）。
 *   ⚠️ 「生成地牢新层」这张表**横跨两个节点**（`map` 7 行 + `outline` 1 行，老板 09-20 拍），
 *      `applies` 落**主体那一侧**（`map`）—— 第 5 列记的是"这张表的小标题出现在哪几处"。
 *
 * 格式（六列，`|` 分隔；`#` 或 `*` 开头是注释）：
 *   `applies原文 | 表名 | 卡 | applies 落哪个节点 | 小标题出现在哪几处（空格分隔） | 为什么`
 *   · ⚠️ 任何一列里**不许出现 `|`** —— 它会让后面的列整体错位
 */

# ==== 6 张旧表的 applies（一条都不许丢）====

晨风镇里还没有具体内容的地点与人物 | 填充晨风镇 | morningwind | map | map | 这张表在 map（段 7a 已拍：镇子的空间归地图）
地牢里尚未生成的层（玩家推进到哪儿，长到哪儿） | 生成地牢新层 | morningwind | map | map outline | 表横跨 map/outline；applies 落主体那一侧（map 7 行、outline 只有节奏那 1 行）
任何有行程的场合（走路、赶夜路、在城里晃） | 生成随机遭遇 | morningwind | outline | outline | 遭遇不改状态、只决定正文怎么写 —— 它只能进大纲的主提示词
需要一个具体的人，而作者没点名时 | 生成新 NPC | morningwind | cast | cast | 段 7a 已拍：角色那一步收第四条
卡里没写、但玩家会走到的舱段与残留物 | 按需长舱段 | long-night | story | story | 孤儿表，老板 09-20 拍：并入正文节点
需要一件从海上来的怪事时 | 海上的东西 | night-watch | story | story | 孤儿表，老板 09-20 拍：与长夜号同形

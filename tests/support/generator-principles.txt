/**
 * 段 7a 的锚点表：**旧 `generators` 表里那 30 行原则** + 每行的**归属**，一条一行。
 *
 * 为什么存成文本而不是 `.ts`：这些是**中文数据**（原则原文与归属理由），
 * 而 `.githooks/checks/ascii.mjs` 对**代码文件**（含 `tests/**`）要求 ASCII ——
 * 中文字面量放 `.ts` 里提交会被拦下。存成 `.txt` 之后：
 *   ① 原文只有一份，不抄进代码；② 用例按行读、逐条断言；③ 格式与 `cards/*.json` 同一族。
 *
 * ⚠️ **这份表是 S1 从改动前的卡里抄下来的**（起点树 `47b58cd`）—— 它记的是**旧形状**：
 *    段 7a 之后卡里就不该再有那张表了。判据 6 钉的就是**行数**。
 *
 * 格式（两列，第一条 `|` 是分隔符；`#` 或 `*` 开头是注释）：
 *   `原则原文 | 锚点 | 卡 | 节点 | 为什么`
 *   · **第 2 列起可省**：省了 = 这一条没有锚点（判据 7/8 跳过它，判据 6 仍然数它）
 *   · 锚点 = 那句原则里**原样连着**的一段（要短，也要在三张卡里唯一）
 *   · ⚠️ 原文里**不许出现 `|`** —— 它会让第二条之后的列整体错位
 *
 * 归属来历（**一个字的推导都不是我定的**）：
 *   · 裁决 R58 `:822-826`：那 4 条搬进干活那一步的主提示词；
 *   · 组长 2026-09-20 拍：「生成地牢新层」按职责拆 —— `map` 拿"这一层长成什么样"、`outline` 拿节奏；
 *   · 老板 2026-09-20 拍：**两张卡的两个孤儿**（长夜号的「按需长舱段」· 夜班的「海上的东西」）
 *     一律 → **正文**节点。
 *   ⚠️ 裁决原文说"地图收前两条"，与卡里的实际不同（**地牢横跨 `map` / `outline` 两个节点**）——
 *      现在这份表是老板按卡的实际重拍的，见契约 §5.2。
 */

# ==== 那 30 行原则（一行一条，一条都不许丢）====

作者点名的五个地点一个不少 | 作者点名的五个地点一个不少 | morningwind | map | 它讲的是镇上该有哪些地点 —— 地图那一步的活
其余店铺、住家按需长，别一次长满 —— 玩家逛到哪儿长到哪儿 | 一次长满 | morningwind | map | 一次长多少是镇上空间的形状
地牢是镇上日常生活的一部分 —— 谈它、赌它、在酒馆里组队下去。别把镇民写成讳莫如深 | 日常生活的一部分 | morningwind | map | 这条讲镇子与地牢的关系，是地图那一步的语境
物价按边境小镇算，别出现金币级的东西（一枚金币在这里是大钱） | 边境小镇 | morningwind | map | 镇子的经济尺度
先看第 1 层「沉水窖」—— 基调和格式都照它来。基调：有年头、有人来过、危险但不致命（不是阴森恐怖那一套）。格式：主题 / 简介 / 场景 / 单位配比 / 通往下一层 / 前人痕迹，一个不少。 | 第 1 层 | morningwind | map | 组长口径：这一层长成什么样归地图
有主题 —— 雨林、火山、冰冻、魔瘴……每层一个，别重复 | 雨林、火山 | morningwind | map | 主题是层的形状
有一条或者几条前进路线，且有明确的前往下一层的方式（击破关底 / 满足某个条件 / 找到某条路） | 前进路线 | morningwind | map | 层的内部结构
有敌意单位、中立单位和友善单位，确保事件多样化而有挑战性 | 中立单位 | morningwind | map | 层的内部构成
与上层连贯：越深越危险、越远离地表；不要连着三层同一个主题 | 越深越危险 | morningwind | map | 层与层之间的关系 = 地图空间的纵深
参考主控当前的实力与状态 —— 别生成必死层，也别生成三只老鼠 | 主控当前的实力 | morningwind | map | 口径是"这一层长成什么样"，但措辞像节奏 —— 见契约 §4.3
一次别生成太多：玩家推进到哪一层，就长到哪一层 | 一次别生成太多 | morningwind | outline | 老板 2026-09-20 拍：节奏归大纲，形状归地图
别交代「总共几层」—— 没人知道。只长玩家走到的那一层 | 总共几层 | morningwind | map | 地图不声明总层数
遭遇要跟「这地方为什么会有这东西」对得上 | 这地方为什么会有这东西 | morningwind | outline | 老板拍：它只能进主提示词 —— 遭遇不改状态，没有动作可挂
绝大部分遭遇是「世俗」的：野狼、土匪、塌方、迷路、一个冻僵的旅人。魔法的东西出现时要有分量 —— 别拿它当路边的野怪 | 世俗 | morningwind | outline | 同上
配比大致三成麻烦、三成机会、四成只是风景（别每段路都有事） | 三成麻烦 | morningwind | outline | 同上
有戏的新角色以年轻女性为主 —— 主控是个年轻男人，「认识人」是这张卡的乐趣之一 | 年轻女性为主 | morningwind | cast | 裁决：角色那一步收第四条（update_role 写 roles）
每个人给一个具体到能记住的特征：口音 / 疤 / 口头禅 / 一个怪癖 / 一门拿手的活 | 能记住的特征 | morningwind | cast | 同上
⭐ 她怎么说话，得写得出来 —— 口头禅 / 句子长短 / 怎么称呼你 / 什么时候闭嘴。写不出说话方式的角色是立牌，不是人 | 怎么说话 | morningwind | cast | 同上
⭐ 不是每个女人都对你有意思 —— 有人嫌你穷、有人嫌你麻烦、有人已经有人了。别把这张卡写成「谁见了你都脸红」 | 都对你有意思 | morningwind | cast | 同上
她有自己的事要办，不是站桩等玩家 | 站桩 | morningwind | cast | 同上
她想要什么，得说得出来 —— 写不出欲望的人，就是背景板 | 想要什么 | morningwind | cast | 同上
人类里会施法的人罕见，能熟练使的更罕见；精灵和龙族会一两手简单的，但一样会累 —— 魔法出现时要有分量，别拿它当路边的野怪 | 会施法的人罕见 | morningwind | cast | 同上
记得写种族 —— 默认是人类，但别让全镇清一色。族类清单里只列了「见过的」五个 —— 需要就随手长新的（魅魔、蜥蜴人、史莱姆娘……） | 记得写种族 | morningwind | cast | 同上
默认长成「次要角色」—— 一行就够（「街角卖烤栗子的老头」），别一上来就给他姓名年龄性格。等玩家表现出想跟他建立关系，再升级 | 次要角色 | morningwind | cast | 同上
作者点名的三个地点一个不少 | 作者点名的三个地点一个不少 | long-night | story | 老板 2026-09-20 拍：并入正文节点的主提示词
其余舱段按需长：玩家往哪走，哪条走廊才存在 —— 别一次把三公里的船长满 | 舱段按需长 | long-night | story | 同上
长出来的东西要有使用痕迹：四百年的划痕、贴纸、手写的便条、修了又修的管线 | 使用痕迹 | long-night | story | 同上
危险写成可处理的具体故障（漏水、断电、卡死的舱门、缺氧），不写会追人的东西 | 可处理的具体故障 | long-night | story | 同上
永远不给解释。 | 永远不给解释 | night-watch | story | 老板 2026-09-20 拍：与长夜号同形，并入正文
先给声音，再给影子，最后才给形状。 | 先给声音 | night-watch | story | 同上
